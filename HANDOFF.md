# Handoff

## Current state

The repository defines two pinned 27B model snapshots, GHCR archive tooling, and
an Ollama API gateway.

## Verification

Local and remote evidence is recorded here as publication proceeds. Do not
claim an OCI artifact is available until ORAS resolves its immutable manifest.

## Remaining work

- Download and publish the two full-precision snapshots.
- Verify both models load in Ollama.
- Verify API calls against a live Ollama service.
